# IEEE118 Strict Fragility GCN

Each non-degenerate strict target is trained with the original RTS-79 `PaperStyleRts79Gcn`. The baseline positive weight is 20; auto weight is `num_negative / num_positive`. Checkpoints stay local-only.
