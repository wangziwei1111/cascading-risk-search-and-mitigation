# IEEE118 prospective physical-oracle run

The frozen gate/probe/GCN policy queried the physical cascade simulator on demand. It did not read an IEEE118 full-truth CSV. Consequently, this directory reports discoveries and physical-call counts, not Recall@K or K90. A later audit may compare the frozen query log with independently generated truth without changing the ranking.
