# IEEE118 Paper-8000 Source Dataset

All shards share the paper-aligned four-feature schema. Shard-normalized arrays are discarded; the merged `x_gcn` is normalized once using only merged train states. Every S1 row stores its explicit active first outage.
